# Session — 2026-03-21 22:47
**Who:** Andrea
**Account:** Cairn Code (CLI)

## What we did

### POC: Archibase + Agent Team → Rhino Geometry (the full evening)

**Three progressive tests in one Rhino file**, proving the knowledge-to-geometry pipeline:

1. **Test 1 — Simple wall** (`POC::` layers, 22 objects)
   - 3 sequential agents: Knowledge → Geometry → Reviewer
   - Proved: archibase queries return real data, wall built in Rhino, every dimension traces to a source
   - All 9 constraints PASS

2. **Test 2 — Full cabin** (`Cabin::` layers, 153 objects → rebuilt as v2)
   - 2 agents: Knowledge+Builder combined, Reviewer separate
   - v1 had issues: course lines only on one face, corners as butting boxes, no roof panels
   - v2 fixed: monolithic ring extrusions for upper courses, double course lines (outer+inner), roof panels with overhang, gable infill
   - Reviewer caught: window too wide for rammed earth 1/3 rule → reduced from 120cm to 100cm

3. **Test 3 — Agent team build** (`Team::` layers, 102 objects)
   - 4 builder agents: Foundation, Walls, Openings, Roof
   - 3 reviewer agents: Constraint, Completeness, Assembly
   - 3 discussion/improvement rounds
   - BUT: used sequential Agent spawns with manual data relay, NOT the real TeamCreate feature
   - Reviews caught: incomplete wall courses 7-14, insufficient roof overhang, missing render cutouts, missing gable infill
   - Round 3 additions: splash apron, door threshold

### Infrastructure built

- **Rhino modeling playbook**: `.claude/agents/knowledge/rhino-playbook.md` — shared modeling techniques, common failures, assembly approach. Merged insights from the Claude's Corner study notes.
- **Domain learning files**: 5 files in `.claude/agents/knowledge/` (foundation, walls, openings, roof, review) — agents write techniques after each build round
- **Updated modeler agent + skill**: both now reference the playbook and Claude's Corner study notes
- **Rhino units changed** from mm to cm for the cabin builds

### Key architectural decisions made

- **Playbook location**: `.claude/agents/knowledge/` — in the shared repo, accessible to both Claudes
- **Learnings are techniques, not logs**: "here's how to make a ring" not "on this date we..."
- **Agent specialization**: system-specialized builders (foundation, walls, openings, roof) + skill-shared playbook
- **Construction knowledge vs modeling knowledge**: archibase = what to build, playbook = how to model it in Rhino
- **The Claude's Corner study notes** should be cross-referenced with archibase — the other Claude session (via Discord) will handle this

## Open threads

- **Real Agent Teams (TeamCreate)**: Andrea wanted parallel agents that talk via mailbox (the actual TeamCreate feature), not sequential spawns with manual relay. This was not tested — save for next session.
- **Claude's Corner study notes → archibase**: The previous Discord session's deep study notes on assemblies/connections need cross-referencing with what archibase already has. That Claude will handle it.
- **Cabin model quality**: Corners on lower courses (with openings) still use multi-box approach, not truly monolithic. Boolean cutting at openings is unreliable in rhinoscriptsyntax.
- **Assembly depth**: Full assembly (render + plaster layers) was added but only as simple boxes — no real layer-by-layer section detail yet.
- **LOCKBOARD needs updating**: Current lockboard doesn't reflect today's POC work or the playbook infrastructure.

## Blockers
- None

## Git state
- Branch: main
- Status: uncommitted changes (see list below)
- Last commits:
  - d1facce [SYNC] Make archibase path configurable
  - 7b2d30f [SYNC] Switch to router MCP config, add Rhino MCP rule
  - f24c9e3 Merge branch 'main'

### Files to commit:
- `.claude/agents/modeler.md` — updated to reference playbook
- `.claude/commands/modeler.md` — updated to reference playbook
- `.claude/agents/knowledge/` — NEW: playbook + 5 learnings files
- `output/city101_hub/poc_archibase_results.md` — POC wall test results
- `output/city101_hub/rhino_scripts/poc_archibase_wall.py` — wall build script
- `output/city101_hub/rhino_scripts/poc_archibase_cabin.py` — cabin v1 script
- `output/city101_hub/rhino_scripts/poc_archibase_cabin_v2.py` — cabin v2 script

## Context to restore

The evening was a progression from simple to complex: single wall → cabin → agent team. Each test revealed what the previous one missed (corners, course lines, missing elements). The playbook and learnings infrastructure was built to capture these discoveries so future agents don't repeat them.

The key insight Andrea had: **there's a difference between construction knowledge (archibase) and modeling knowledge (how to drive Rhino)**. Both are needed. The playbook bridges the gap.

Andrea wants the NEXT session to use **real Agent Teams (TeamCreate)** — persistent agents with shared task lists and mailbox communication. The sequential relay pattern works but doesn't test true parallel coordination. The cabin spec, layers, playbook, and learnings are all ready — a fresh session can jump straight to TeamCreate.

Also: Andrea and the Discord Claude session are coordinating — the Discord Claude will cross-reference the study notes with archibase while this session captured the modeling playbook.

## Resume with
→ **Try real Agent Teams (TeamCreate) for the cabin build.** Everything is set up: layers exist, playbook exists, learnings exist, archibase data is known. The next session just needs to use TeamCreate instead of sequential Agent spawns, and let the agents coordinate via SendMessage mailbox.
