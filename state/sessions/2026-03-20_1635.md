# Session — 2026-03-20 16:35
**Who:** Andrea
**Account:** Cairn Code (CLI)

## What we did
- **Verified Rhino MCP connection** and confirmed Lock 05 v4 geometry (709 objects) still intact across all 10 layers
- **Attempted to run v5 script** (`lock_05_chuv_gradient_v5_agent_team.py`) — deleted existing geometry, executed script, 709 objects created with matching counts. Script runs but geometry differs from original agent team build (it's a reconstruction, not a capture)
- **Discussed output strategy** for agent team builds: the fundamental tension between live MCP building (good for coordination) and reproducible scripts (good for the app). Three paths identified: Spec→Codifier, Geometry Bake, .3dm file
- **Andrea's key insight**: team builds live in Rhino as workspace, produces a precise spec sheet, then a separate codifier agent writes the Python script from the spec
- **Created `workflows/agent-team-modeling-v2.md`** — preserves v1 (lived experience), adds:
  - Phase 5: three output paths with comparison table
  - Coordinator guide: time-boxing, discuss→decide→execute pattern
  - Idle-time protocol with 5 rules: observation mode, questions-not-opinions, reassign, round trigger with pause for last agent, simultaneous proposals
  - Anti-patterns table, 3-4 round rule, diminishing returns data
- **Designed collaboration rules**: agents enter observation mode when idle (private notepad), can ask factual questions but not share opinions (prevents anchoring bias from first-to-finish), last agent gets pause + shares build report before proposals open

## Open threads
- **v5 script doesn't match original geometry** — it's a reconstruction from bounding boxes, not a capture. The output strategy paths in v2 address this
- **Bake tool not built yet** — Path B from the workflow (automated geometry export) would be easy to build
- **Path A (Spec→Codifier) untested** — most promising path for the app but hasn't been tried
- **Untracked files** still in repo: `output/app_architecture/`, `experiments/`, `prompts/`, etc.

## Blockers
- None

## Git state
- Branch: andrea/prototypology-v2
- Status: modified session-log.md, various untracked files in output/experiments/prompts
- Last commits:
  - 892385b [MODEL] Agent team modeling workflow v2 — output strategy + coordinator guide
  - 29a0b8c [MODEL] Lock 05 CHUV: facade base reveal + canopy drip edge
  - 2003b56 [SYNC] Session end — Rhino router + agent teams proven

## Context to restore
- The v2 workflow is speculative — none of the idle-time rules or output paths have been tested in a real team build yet
- The next team build should test: observation mode, questions-not-opinions, simultaneous proposals, and Path A (spec→codifier)
- Lock 05 v5 script exists and runs (709 objects, correct counts) but produces different geometry than the original agent team build

## Resume with
→ Next team build on a different lock — test the v2 workflow rules and Path A (spec→codifier) output strategy
