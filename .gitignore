# OS files
.DS_Store
Thumbs.db

# Python
__pycache__/
*.pyc
*.pyo
.env
.venv/
venv/

# Node (if any viz tooling)
node_modules/

# IDE
.vscode/
*.swp
*.swo
*~

# Local Claude Code settings (personal config)
.claude/settings.local.json

# Temporary / staging
output/tmp/

# Large binaries — keep local, not in git (saves LFS quota)
*.pptx
*.docx
*.gpkg
*.qgz
*.3dm
*.blend
*.blend1
*.pdf
*.png
*.jpg
*.jpeg
*.tif
*.tiff
*.zip
*.mp4
*.mov
*.avi
*.gif
*.rhl

# swissBUILDINGS3D extracted DXFs (can be >100MB, keep .dxf.zip instead)
output/city101_hub/context/*swissbuildings3d.dxf

# Credentials — never commit these
*.key
*.pem
credentials.json
.cookies.json

# Vector databases (large, rebuilt from embeddings)
output/dicobat/vectordb/
tools/vectordb/

# Construction knowledge database (rebuilt from seed data)
output/knowledge/construction.db

# Obsidian metadata
.obsidian/
*.canvas

# Terminal saved output (not for git)
CLAUDE DOWN SAVE/

# Extracted geodata site files (re-extract per machine)
geodata/sites/
